import React from 'react';
import logo from '../../assets/logo.png';

const Header = () => (
  <header>
    <img src={logo} alt="MLBB Diamond Store" />
    <h1>MLBB Diamond Store</h1>
  </header>
);

export default Header;